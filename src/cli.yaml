name: sg
version: "1.0"
subcommands:
  - product:
      about: Sets the session enviroment variable for the product
      args:
        - product_name:
            help: Product short name(code)
        - create_product:
            short: c
            long: create
            takes_value: true
            help: Create a product
